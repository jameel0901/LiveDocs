# Copy to .env.local for local development overrides.
# For production builds, copy .env.production.example to .env.production
# or set these in your CI / hosting build environment.
# Variables must be prefixed with REACT_APP_ to be embedded in the build.

# Local backend
REACT_APP_API_BASE_URL=http://localhost:5000
REACT_APP_SOCKET_URL=http://localhost:5000
